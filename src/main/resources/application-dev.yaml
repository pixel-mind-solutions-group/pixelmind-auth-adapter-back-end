server:
  port: 9001

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pixelmind-auth-adapter-management
    username: keycloak26
    password: PixelmindSriLanka
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update